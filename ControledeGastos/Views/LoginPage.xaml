<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="ControledeGastos.Views.LoginPage"
             Shell.NavBarIsVisible="False">
    <ContentPage.Content>
        <StackLayout Padding="10,0,10,0" VerticalOptions="Center">
            <Label Text="Email"/>
            <Entry Text="{Binding EntEmail}" Keyboard="Email"/>
            <Label Text="Senha"/>
            <Entry Text="{Binding EntSenha}" IsPassword="True"/>
            <Button VerticalOptions="Center" Text="Login" Clicked="LoginButton_Clicked"/>
            <Button Clicked="ForgotPassword_Clicked" Text="Esqueci minha senha" />
            <Button Clicked="ForgotPassword_Clicked" Text="Novo Usuario"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
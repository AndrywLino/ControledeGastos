<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="ControledeGastos.Views.ForgotPasswordPage"
             Title="{Binding Title}">

    <ContentPage.Content>
        <StackLayout Padding="10">
            <Label Text="Sua senha sera enviada por email." FontAttributes="Bold" HorizontalTextAlignment="Center" VerticalTextAlignment="Center" Padding="10"/>
            <Label Text="Digite seu email"/>
            <Entry Text="{Binding EntEmail}" Placeholder="Email"/>
            <Button Text="Enviar email" Command="{Binding SendEmailCommand}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>

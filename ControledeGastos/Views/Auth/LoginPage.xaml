<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="ControledeGastos.Views.LoginPage"
             Shell.NavBarIsVisible="False">
    <ContentPage.Content>
        <StackLayout Padding="10,0,10,0" VerticalOptions="Center">
            <Label Text="Email"/>
            <Entry Text="{Binding EntEmail}" Keyboard="Email" Placeholder="Email"/>
            <Label Text="Senha"/>
            <Entry Text="{Binding EntSenha}" IsPassword="True" Placeholder="Senha"/>
            <Button VerticalOptions="Center" Text="Login" Command="{Binding LoginCommand}"/>
            <Button Text="Esqueci minha senha" Command="{Binding ForgotPasswordCommand}" />
            <Button Text="Novo Usuario" Command="{Binding NewUserCommand}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>